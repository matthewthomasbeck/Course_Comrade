<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Toolbar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/white"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        android:popupTheme="@style/ThemeOverlay.AppCompat.Light">

        <!-- Profile Button -->
        <ImageButton
            android:id="@+id/buttonProfile"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/baseline_person_outline_24"
            android:contentDescription="@string/profile"
            android:background="?attr/selectableItemBackground"
            android:layout_gravity="end"
            android:padding="8dp" />

        <!-- Toolbar Title -->
        <TextView
            android:id="@+id/toolbarTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Meet the Creators"
            android:textSize="20sp"
            android:gravity="center"
            android:textColor="@color/black" />
    </androidx.appcompat.widget.Toolbar>

    <!-- GitHub Links Container -->
    <LinearLayout
        android:id="@+id/githubLinksContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_below="@id/toolbar">

        <!-- Example GitHub Link 1 -->
        <TextView
            android:id="@+id/githubLink1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Matthew Beck's GitHub"
            android:textSize="18sp"
            android:textColor="@color/blue_shade_1"
            android:padding="8dp"
            android:clickable="true"
            android:focusable="true" />

        <!-- Example GitHub Link 2 -->
        <TextView
            android:id="@+id/githubLink2"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Creator 2's GitHub"
            android:textSize="18sp"
            android:textColor="@color/blue_shade_1"
            android:padding="8dp"
            android:clickable="true"
            android:focusable="true" />

        <!-- Example GitHub Link 3 -->
        <TextView
            android:id="@+id/githubLink3"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Creator 3's GitHub"
            android:textSize="18sp"
            android:textColor="@color/blue_shade_1"
            android:padding="8dp"
            android:clickable="true"
            android:focusable="true" />

        <!-- Example GitHub Link 4 -->
        <TextView
            android:id="@+id/githubLink4"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Creator 4's GitHub"
            android:textSize="18sp"
            android:textColor="@color/blue_shade_1"
            android:padding="8dp"
            android:clickable="true"
            android:focusable="true" />
    </LinearLayout>
</RelativeLayout>
